<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.modules.sys.dao.SysColumnDao">

    <!-- 字段集 -->
    <sql id="baseSql">
        select column_id, user_id, table_id, content from sys_column
    </sql>

    <!-- 查询列表字段 -->
    <select id="queryList" resultType="com.platform.modules.sys.domain.SysColumn">
        <include refid="baseSql"/>
        <where>
        </where>
    </select>

</mapper>
